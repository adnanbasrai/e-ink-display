// NYC Subway arrivals board -- see README.md.
//
// All code lives in board_main.cpp (and the other .cpp files); this stub
// exists because the Arduino IDE requires a .ino matching the folder name.
// Keeping logic out of the .ino also keeps the sketch preprocessor from
// rewriting it.
